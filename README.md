for install npm install

for run the code : npm run dev

```
- Display the first 9 items in a 3 x 3 grid below the search box.
- After Searching the item and enter in the search box, that search item only shows.
- We can close recent search box When we click outside the box.
- When we start typing they will show an Available item in the box, we can click on that list and search those items.
- After re-clicking on the search field then recent search items will show, we can click on that list and search those items.
- If you want to clear recent search items, we can do this after clicking the clear search button.
- After clicking items, navigate to the new page of that item's id.

```
